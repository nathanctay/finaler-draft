// Fixture asset standing in for a real Vite content-hashed bundle (e.g. index-CD6YQ5bG.js), used
// only to prove the static-caching headers in app.ts's fastifyStatic registration -- see
// app.test.ts's "production client serving" tests. Content is inert on purpose: this file is
// never executed, only served and inspected for response headers.
export const fixtureAsset = true;
